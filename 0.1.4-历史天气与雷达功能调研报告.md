# 枳生天气 0.1.4：历史天气与雷达功能调研报告

> 调研日期：2026-08-28  
> 当前基线：`release/v0.1.3` / `a9071139d5886ac40d9da57b26b0255bf05b1893`  
> 开发状态：调研后已完成 0.1.4 功能实现、与旧功能联动、自动化验收和公开并行版真机覆盖安装；未修改任何其他历史版本目录。

## 零、开发与验收结果（2026-08-28）

- 版本已升级为 `0.1.4 / 20260832`，开发分支为 `release/v0.1.4`。
- 首页新增一个独立编号的「时空观测」模块，内部合并「往年同日」「雷达回波」两个入口。模块整体纳入原有主页排序和显示开关；升级时保留旧用户的自定义顺序，后续新功能仍可继续独立插入。
- 往年回顾已实现近5/10年切换、同日高低温航迹、降水/风速、均值摘要、闰年处理、部分失败保留和长期本机缓存；跟随当前城市、温度单位、风速单位和主题。5/10年切换时保留原内容并在卡内补齐，不再出现整屏夹帧。
- 历史数据已由字段不完整的强制 ERA5-Land 改为 Open-Meteo Best Match；已实测金昌 2021—2025 年同日天气码、高低温、降水和风速均有效返回。旧空字段缓存通过新缓存版本自动失效，全空响应不再当作成功数据。
- 雷达页已实现最新帧优先、最多13帧逐批补齐、默认自动播放、单帧点选、播放/暂停、时间轴、双指1—4倍缩放与拖动、双击复位、中心位置、距离环、短时缓存、旧元数据降级和中央气象台官方页备用入口。
- 页面延用枳生天气的直角终端卡、磷光绿/信号橙/线框青、现有字体与深浅两套主题；雷达仅在播放时使用一处扫描动效。
- 单元测试：216项全部通过。Android Lint：0错误，85条为项目原有提示，新功能文件无新增 Lint 问题。
- 公开并行版 APK 已构建和覆盖安装：`com.zhisheng.weather.preview / 0.1.4 / 20260832`；和风内置凭据为空，自更新能力关闭，未安装满血版。

## 一、直接结论

1. **往年历史天气回顾：建议做，可直接进入 0.1.4。**
   - 首选 Open-Meteo Historical Weather API（Archive API）。它免 Key、全球覆盖、最早可查至 1940 年，项目已有 Open-Meteo、OkHttp 和 Kotlin Serialization 基础，新增代码量可控。
   - 必须把页面数据标成“历史再分析”，不能冒充气象站实测记录；免费公共 API 仅允许非商业使用，每天最多 10,000 次请求且无 SLA。
   - 第一版建议显示“当前城市同月同日近 5 年回顾”，每年一条：天气现象、最高/最低温、降水、最大风速，并给出最暖、最冷、最湿年份。历史结果长期缓存。

2. **雷达图：技术上能做，但“国内免费”和“绝对保证”不能同时由第三方免费服务承诺。**
   - 当前最可行的是 RainViewer Weather Maps API：免 Key、面向个人/教育/小型社区免费；其官方覆盖页列出中国约 197 部雷达。
   - 2026 年免费 API 已收缩为：过去约 2 小时、10 分钟间隔、最大缩放级别 7、每 IP 每分钟 100 请求；未来雷达外推已经取消。因此页面只能叫“雷达回波”或“过去 2 小时雷达”，不能写“未来雷达预测”。
   - 2026-08-28 在当前网络实测：元数据、Open-Meteo Archive、北京/金昌/上海雷达帧均返回 HTTP 200；单帧约 12–24 KB。项目现有 `radar-preview` 三张预览也能看到有效回波。
   - RainViewer 明确不保证数据持续存在和服务可用；中国回波的上游来自中央气象台，而中央气象台网站同时写明未经授权禁止下载使用。RainViewer 也提示上游数据权利取决于原始提供方。因此公开发行前应向 RainViewer/NMC 确认中国数据的再分发授权，至少保留书面答复。
   - 建议把雷达做成可替换的 `RadarProvider`，默认 RainViewer；失败时显示最近缓存和“打开中央气象台官方雷达页”按钮，不要私自抓取 NMC 图片作为备用源。

3. **0.1.4 文件夹已准备好。**
   - 从 `ZhishengWeather-0.1.3` 完整复制为 `ZhishengWeather-0.1.4`：14,593 个文件、805.51 MiB，复制过程 0 失败、0 不匹配。
   - 两个目录的 Git HEAD 相同，均为干净的 `release/v0.1.3`；0.1.3 没有被修改。
   - 0.1.4 基线执行 `testDebugUnitTest`：37 个测试套件、208 项测试、0 失败、0 错误、0 跳过，`BUILD SUCCESSFUL`。

## 二、0.1.3 项目现状

### 1. 技术栈与发布方式

- 原生 Android 单模块应用，Kotlin 2.0.21、Jetpack Compose、AGP 8.5.2、JDK 17，最低 Android 8.0（API 26）。
- 网络层：OkHttp + Retrofit + Kotlin Serialization；设置与城市数据使用 DataStore；天气离线兜底使用本地 JSON 缓存；桌面小组件通过 WorkManager 每小时同步。
- 单 Activity、Compose 自建页面栈。目前页面是 HOME、SEARCH、SETTINGS、ATMOSPHERE_LAB；历史和雷达适合各自增加独立覆盖页，避免继续膨胀 2,742 行的 `HomeScreen.kt`。
- 公开版、私有满血版、性能版、公开并行安装版四种构建类型；公开版在构建期强制清空和风凭据。GitHub CI 会跑单测、Lint、Debug 和 Public Release 构建。
- 当前正式版内部版本为 `versionCode=20260831`、`versionName=0.1.3`。本轮没有提前修改 `update.json`、发布 APK 或版本说明，避免生成一个名义上的空 0.1.4 发布状态。

### 2. 当前数据架构

- `WeatherRepository` 统一调度和风、彩云、小米公开接口、Open-Meteo 四个数据源。
- 自动优选链以小米为主，Open-Meteo 只补实况遥测、逐时/逐日缺项；小米整源失败时才切到 Open-Meteo。手动锁源保持单一来源。
- `WeatherData` 已包含实况、逐时、逐日、空气质量、预警、分钟降水、生活指数、昨日、台风和分块来源，但没有日期区间历史模型，也没有雷达帧模型。
- 首页的可排序模块目前有逐时、短时降水、逐日、遥测、AQI、生活指数、昨日复盘、台风。历史/雷达如果做成首页整块，会进一步增加首屏网络和状态复杂度；更合适的是首页入口卡 + 独立页面。
- 当前 `YesterdayInfo` 只来自小米辅助字段，不等于“往年历史”；不能在其上硬扩展多年数据。

### 3. 对 0.1.4 的结构建议

- 不把多年历史和雷达图片塞进 `WeatherData`，避免天气刷新、离线天气缓存、小组件快照一起变重。
- 建议新增两个独立纵向模块：
  - `history/`：API、模型、Repository、缓存、ViewModel、Screen。
  - `radar/`：Provider 接口、RainViewer 实现、帧缓存、ViewModel、Screen。
- `MainActivity` 的页面枚举增加 HISTORY、RADAR；首页增加一个紧凑的“双入口卡”，或者在顶部操作区增加入口。进入独立页后才发请求。
- 版本号、更新弹窗、README、`update.json` 和发布脚本参数在功能完成并通过发行门禁后统一升级，不在设计阶段提前改。

## 三、往年历史天气回顾实现方案

### 1. 数据源选择

首选：[Open-Meteo Historical Weather API](https://open-meteo.com/en/docs/historical-weather-api)

- `/v1/archive` 按经纬度和日期查询，不需要 API Key。
- 1940 年至今可用；ERA5-Land 约 0.1°，ERA5 约 0.25°，近年还可用更高分辨率 IFS 数据。
- 推荐日变量：`weather_code`、`temperature_2m_max`、`temperature_2m_min`、`temperature_2m_mean`、`precipitation_sum`、`wind_speed_10m_max`、`wind_gusts_10m_max`。
- 使用 `timezone=auto`，复用现有 WMO 天气码映射和单位换算。
- 页面固定显示数据源、模型和“历史再分析”说明。Open-Meteo 数据为 CC BY 4.0，必须署名并链接许可。

不建议把和风“时光机”作为多年历史主源：官方 API 只支持最近 10 天；2000 年以来的长时段数据需要联系销售获取文件，不适合客户端按需回顾。

### 2. 产品形态

第一版建议固定成“这一天，往年如何”：

- 默认近 5 年，避免一次页面打开就产生 10 次以上请求。
- 每年显示一行天气现象、高低温、降水和最大风速。
- 顶部给出近 5 年均值、最高/最低和最大降水年份。
- 允许切换到近 10 年，但先提示需要加载更多历史数据。
- 2 月 29 日只显示真实闰年，不把 2 月 28 日或 3 月 1 日伪装成同一天。
- 城市切换、页面退出时取消旧协程，沿用现有 ViewModel 防乱序模式。

### 3. 请求和缓存

- 同一经纬度、日期、模型的历史结果基本不再变化，应缓存到 `cacheDir/history`，建议 30 天后仅后台复核，离线时长期可读。
- 同一天近 5 年需要 5 个离散日期请求；并发不超过 3，失败年份单独重试，不让一条失败拖垮整页。
- 不要请求连续 5 年全量日数据：响应巨大，Open-Meteo 也会按时间跨度折算更多调用量。
- 数据模型应保留 `source`、`model`、`fetchedAt` 和 `isStale`，展示缓存年龄。

### 4. 风险

- Open-Meteo 免费 API 只允许非商业应用，限 10,000 次/日、5,000 次/小时、600 次/分钟，无 SLA。若未来加入广告、订阅或商业发行，需要改用商业计划或自建服务。
- 再分析数据不是具体气象站逐时观测，山区、局地强对流和短时降水会有偏差；文案必须保守。
- 当前客户端直连意味着用户所选城市坐标会发送给 Open-Meteo，隐私说明要随 0.1.4 更新。

## 四、雷达图实现方案

### 1. 推荐数据链路

首选：[RainViewer Weather Maps API](https://www.rainviewer.com/api/weather-maps-api.html)

1. 请求 `https://api.rainviewer.com/public/weather-maps.json` 获取帧时间和当前 tile host。
2. 取 `radar.past` 帧，按城市经纬度请求坐标居中的 PNG：`{host}{path}/256/7/{lat}/{lon}/2/1_1.png`。
3. 首屏先显示最新一帧，再顺序加载相邻帧；全部帧就绪后播放过去约 2 小时动画。
4. 元数据最多每 5 分钟刷新一次，帧图片以 `path + lat/lon + zoom` 为键写入 `cacheDir/radar`。

这一路径不需要地图 SDK，也不需要在开源仓库放第三方 Key。直接坐标图通常每帧只请求一张图，比交互地图的多瓦片方案更容易遵守 100 请求/IP/分钟限制。

### 2. UI 建议

- 页面标题使用“雷达回波”，同时写明“过去 2 小时 · 10 分钟一帧”。
- 最新帧、播放/暂停、时间轴拖动、城市中心标记、方向和距离环是第一版必需项。
- RainViewer 返回的是透明回波层。第一版建议用本地终端风网格、经纬网、方位和距离环作为底图，不接 Google 地图，也不抓取未经授权的 NMC 底图。
- 如果一定要完整道路地图，可另接高德 Android 地图 SDK；但它要求开发者实名、Key、配额和隐私合规。项目公开签名证书本身是公开的，嵌入高德 Key 也更容易被他人复用，因此不建议作为 0.1.4 默认方案。
- 必须在雷达页可见位置显示 RainViewer 署名和链接。

### 3. 国内可用保障策略

免费第三方不能给出绝对 SLA，但可以把实际可用性做得稳：

- 只在用户打开雷达页时请求，不跟随天气首页刷新。
- 元数据失败时读取最近成功帧，清楚标“缓存”和时间；缓存过旧则不继续播放。
- 并发不超过 3；首帧优先；快速切城市时取消旧请求；连续失败进行指数退避。
- 保留 `RadarProvider` 抽象，避免 RainViewer 接口再次收缩时重写 UI。
- 服务不可用时提供“打开中央气象台官方雷达”外链；不直接下载或二次分发 NMC 图片。
- 发布前用中国移动、联通、电信及常见家庭宽带做至少 3 地网络验收；当前单机测试只能证明“此刻可达”，不能代替全国网络保证。

### 4. 明确不做

- 不把过去回波叫作“未来预测”。
- 不直接抓取 NMC 网页图片当免费 API。
- 不使用 Google Maps 作为国内默认底图。
- 不用一次性全屏多瓦片 × 13 帧的加载方式冲击 RainViewer 限流。
- 不在首页后台预加载雷达，不让雷达影响现有天气首屏速度。

## 五、建议开发顺序与验收门禁

### 阶段 A：历史天气

1. 新建历史 API、模型、Repository 和磁盘缓存。
2. 新建历史页面与 ViewModel；接入当前城市和单位设置。
3. 增加 2 月 29 日、时区、WMO 映射、部分年份失败、缓存过期测试。
4. 更新数据来源、隐私和署名文案。

### 阶段 B：雷达 MVP

1. 新建 `RadarProvider` 和 RainViewer 元数据/帧解析。
2. 实现最新帧优先、缓存、限流、取消和失败降级。
3. 实现终端风雷达页、时间轴、播放控制、来源署名与官方页外链。
4. 增加帧排序、过期缓存、限流、城市切换和无覆盖测试。
5. 完成 RainViewer/NMC 中国数据再使用许可核实后，才进入公开版默认开启状态。

### 阶段 C：发行

- 依次执行 `testDebugUnitTest`、`lintDebug`、`assembleDebug`、`assemblePublicRelease`。
- 真机检查竖屏、横屏待机、城市切换、离线缓存、深浅主题和低网速。
- 最后统一升级 `versionCode`、`versionName`、更新弹窗、README、`update.json`、APK 和 SHA256。

## 六、最终建议

- **历史天气：批准开发。** 采用 Open-Meteo Archive，先做近 5 年同日回顾，明确标注“历史再分析”。
- **雷达：有条件批准 MVP。** 采用 RainViewer 坐标帧 + 本地终端底图，不接 Google、不抓 NMC；公开发布前补齐中国数据授权确认和三网测试。
- **如果要求“永久免费 + 全国绝对稳定 + 可商用 + 无授权风险”四项同时成立，目前没有合格的第三方方案。** 达到这种保证只能走获得授权的中国气象数据服务并自建/签约高可用后端，这会引入服务器和运维成本，不再是零成本方案。

## 七、主要官方资料

- [Open-Meteo Historical Weather API](https://open-meteo.com/en/docs/historical-weather-api)
- [Open-Meteo Terms](https://open-meteo.com/en/terms)
- [Open-Meteo Licence](https://open-meteo.com/en/license)
- [和风天气时光机](https://dev.qweather.com/docs/api/time-machine/time-machine-weather/)
- [RainViewer API](https://www.rainviewer.com/api.html)
- [RainViewer Weather Maps API](https://www.rainviewer.com/api/weather-maps-api.html)
- [RainViewer 2026 API 迁移说明](https://www.rainviewer.com/api/transition-faq.html)
- [RainViewer 中国雷达覆盖](https://www.rainviewer.com/radars/china.html)
- [中央气象台雷达页](https://www.nmc.cn/publish/radar.html)
- [中国气象数据网雷达共享服务](https://k.data.cma.cn/mekb/?r=radar)
- [高德开放平台服务协议](https://lbs.amap.com/pages/terms/)
